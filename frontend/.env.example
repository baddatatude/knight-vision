# Copy to .env.development.local (gitignored) or set when building.

# Leave empty for local dev: Vite proxies /api to FastAPI (see VITE_DEV_PROXY_TARGET).
# VITE_API_BASE=

# Production / Railway single service (same domain as API — leave empty):
# VITE_API_BASE=
#
# Split API-only host (unusual for this repo’s Dockerfile):
# VITE_API_BASE=https://knight-vision-production.up.railway.app

# Where Vite dev proxy sends /api (default FastAPI on this machine)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
